{
  "asi"          : true, // 为真允许自动分号插入
   "bitwise"      : true, // 为真表示允许位运算
   "boss"         : false, // 为真表示不允许if for while 里写赋值语句
   "browser"      : true, // 为真表示为浏览器环境
   "couch"        : true, // 为真表示为couchdb环境
   "curly"        : false, // 为真表示if while 时 必须加 {}
  "debug"        : false, // 为真表示允许出现debugger
  "devel"        : true, // 为真表示允许console，alert出现
  "eqeqeq"       : false, // 为真表示只使用 === 
  "eqnull"       : true, // 为真表示允许使用 == null 
  "es5"          : true,  // 为真表示允许使用es5 语法
  "evil"         : true, // 为真表示不允许使用eval
  "expr"         : true, //if ExpressionStatement should be allowed as Programs
  "forin"        : true, // 为真，则允许for in里不出现 hasOwnProperty
  "globalstrict" : false,  //if global "use strict"; should be allowed
  "immed"        : false, // 为真的话匿名函数要求为 (function(){}()); 而不是  (function(){})();
  "jquery"       : true, // if jQuery globals should be predefined
  "latedef"      : true, // if semicolons may be ommitted for the trailing statements inside of a one-line blocks.
  "laxbreak"     : true, // 为真则不检查换行
  "loopfunc"     : true,  //if functions should be allowed to be defined within loops
  "mootools"     : false, // if MooTools globals should be predefined
  "multistr"     : true, // allow multiline strings
  "newcap"       : true, // 为真则要求构造函数名以大写开头
  "noarg"        : true, // 为真则禁止使用  arguments.caller和arguments.callee
  "node"         : false, // if the Node.js environment globals should be predefined
  "noempty"      : true, // 为真则禁止出现空的代码块，也禁用构造器
  "nonew"        : true,  // if using `new` for side-effects should be disallowed
  "nomen"        : false, // 为真则禁用下划线变量名
  "onevar"       : false, // 为真连续的变量只用1个var
  "passfail"     : false, // 为真则在发现收割错误后停止检查
  "plusplus"     : false, // 为真则禁用自增和自减运算
  "prototypejs"  : false, // if Prototype and Scriptaculous globals should be predefined
  "regexdash"    : true, //if unescaped first/last dash (-) inside brackets should be tolerated
  "regexp"       : false, // 为真则不允许使用.和[^...]的正则
  "rhino"        : false, // if the Rhino environment globals should be predefined
  "undef"        : true, // 为真则要求所有的非全局变量在使用前声明 
  "shadow"       : true, // if variable shadowing should be tolerated 允许
  "strict"       : false, // 是否使用 strict 语法
  "sub"          : true, // 为真则允许各种形式的下标来访问对象
  "supernew"     : true, // if `new function () { ... };` and `new Object;`  should be tolerated
  "trailing"     : true, // if trailing whitespace rules apply
  "white"        : false, // 为真则依据严格的空白检查规范检查代码
  "wsh"          : false, // if the Windows Scripting Host environment globals
  "indent"       : 4,

  "predef"     : [
    "jasmine",
  "describe",
  "xdescribe",
  "it",
  "xit",
  "beforeEach",
  "afterEach",
  "expect",
  "spyOn",
  "runs",
  "waits",
  "waitsFor",
  "Benchmark",
  "Raphael",
  "Backbone",
  "Modernizr",
  /* our var begin */
  "define",
  "App",
  "warnTip",
  "infoTip",
  "mailAddressClass",
  "Profile",
  "signeditor",
  "returnWeatherThreeDays", // 这个变量时返回天气的url返回的数据里定义的变量
  "returnCitySN",           // 这个是返回用户ip地址的url里返回的数据里定义的变量
  "Tips",
  "IM",
  "feditor",
  "imeClose",
  "loginIM",
  "ActiveXObject",
  "sogou",
  "Ember",
  "Em"

    ]
}
